class Routeur{
  String nom;
  double x;
  double y;
  int etage;
  int force = 0;
  double distance = 0.0;
  Routeur(this.nom, this.etage, this.x, this.y);
  //Routeur(this.nom, this.etage, this.x, this.y, this.distance, this.force)
}